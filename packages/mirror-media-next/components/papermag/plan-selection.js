import styled from 'styled-components'

const PlanWrapper = styled.section`
  min-height: 518px;
  background-color: rgba(0, 0, 0, 0.05);
`

function Plan() {
  return <PlanWrapper>方案選擇</PlanWrapper>
}

export default Plan
